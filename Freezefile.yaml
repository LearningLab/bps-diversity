common:

    database: sqlite:///bps.db
    prefix: data/
    format: csv

exports:

  - query: "SELECT * FROM schools WHERE total > 0 AND district LIKE 'boston'"
    filename: "{{year}}.csv"
    mode: item